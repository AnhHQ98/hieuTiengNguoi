import classNames from 'classnames/bind';
import styles from './FilmScene.module.scss';
const cx = classNames.bind(styles);

function FilmScene2({ scenes_json, frames }) { 


    return (
        <div>

        </div>
    )
}

export default FilmScene2;